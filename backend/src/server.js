import express from "express";import dotenv from "dotenv";import path from "path";import {testDatabaseConnection} from "./config/db.js";import productRoutes from "./routes/productRoutes.js";import supplierRoutes from "./routes/supplierRoutes.js";import uploadRoutes from "./routes/uploadRoutes.js";import categoryRoutes from "./routes/categoryRoutes.js";import authRoutes from "./routes/authRoutes.js";import paymentRoutes from "./routes/paymentRoutes.js";import {requireAuth} from "./middleware/auth.js";
dotenv.config();const app=express();const PORT=process.env.PORT||5000;const FRONTEND_URL=process.env.FRONTEND_URL||"http://localhost:5173";
app.use((req,res,next)=>{res.header("Access-Control-Allow-Origin",FRONTEND_URL);res.header("Access-Control-Allow-Methods","GET, POST, PUT, PATCH, DELETE, OPTIONS");res.header("Access-Control-Allow-Headers","Content-Type, Authorization");if(req.method==="OPTIONS")return res.sendStatus(200);next()});app.use(express.json());app.use("/uploads",express.static(path.resolve("uploads")));app.get("/api/health",(req,res)=>res.json({status:"ok",message:"WeConnect backend is running"}));app.get("/",(req,res)=>res.json({message:"WeConnect backend is running"}));app.use("/api/auth",authRoutes);app.use("/api/products",requireAuth,productRoutes);app.use("/api/supplier",requireAuth,supplierRoutes);app.use("/api/uploads",requireAuth,uploadRoutes);app.use("/api/categories",requireAuth,categoryRoutes);app.use("/api/payments",paymentRoutes);app.use((err,req,res,next)=>{console.error(err);res.status(err.status||500).json({message:err.message||"Internal server error"})});app.listen(PORT,async()=>{console.log(`WeConnect server running on http://localhost:${PORT}`);try{await testDatabaseConnection()}catch(error){console.error("MySQL connection failed:",error.message)}});